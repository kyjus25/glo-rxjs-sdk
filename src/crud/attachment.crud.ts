import {RequestService} from '../request.service';
import {HttpClient} from '@angular/common/http';

export class AttachmentCrud extends RequestService {

  public get(boardId, cardId) {
    console.log('set');
  }
  public create(boardId, cardId) {
    console.log('set');
  }
}
