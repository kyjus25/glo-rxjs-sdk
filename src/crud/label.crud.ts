import {RequestService} from '../request.service';

export class LabelCrud extends RequestService {

  public create(boardId) {
    console.log('set');
  }
  public update(boardId) {
    console.log('set');
  }
  public delete(boardId) {
    console.log('set');
  }
}
